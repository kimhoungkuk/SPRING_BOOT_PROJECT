spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  #datasource:
   # driver-class-name: org.h2.Driver
   # url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
   # username: sa
   # password: sa
  datasource:
    initialization-mode : always
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://10.0.75.1:3306/test
    username: test
    password: kimhoung0  
  h2:
    console:
      enabled: true
#    http://localhost:8080/h2-console
